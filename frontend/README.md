# frontend — Bhavan price estimator UI

React + TypeScript + Vite frontend for the House Price Prediction project.
See the [root README](../README.md) for full setup and architecture docs.

Quick start:

```bash
npm install
cp .env.example .env
npm run dev
```
